#include <bits/stdc++.h>
using namespace std;
int main()
{
    //  string s1 = {"Hello"};
    // string s2 = {"Bello"};
    // s1 = s1 + s2; // append another string. s1+=s2 same kotha
    // s1.append(s2);
    // s1.push_back('x'); // add charecter to the last of the string
    // s1.pop_back(); // remove the last character from the string
    string a = {"Showrab"};
    //   a.erase(2, 1); // 2 is the index of specific character and 1 is how many char
    // a.replace(2, 1, "x");
    // a.insert(3, "xoxo");
    cout
        << a;
    return 0;
}